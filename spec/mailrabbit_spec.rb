require 'spec_helper'

describe Mailrabbit do
  it 'has a version number' do
    expect(Mailrabbit::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
